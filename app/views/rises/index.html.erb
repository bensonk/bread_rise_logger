<h1>Listing rises</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @rises.each do |rise| %>
  <tr>
    <td><%= rise.name %></td>
    <td><%= rise.description %></td>
    <td><%= link_to 'Show', rise %></td>
    <td><%= link_to 'Edit', edit_rise_path(rise) %></td>
    <td><%= link_to 'Destroy', rise, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Rise', new_rise_path %>
