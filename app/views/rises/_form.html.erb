<%= form_for(@rise) do |f| %>
  <% if @rise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rise.errors.count, "error") %> prohibited this rise from being saved:</h2>

      <ul>
      <% @rise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :base_height %><br />
    <%= f.text_field :base_height %>
  </div>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
